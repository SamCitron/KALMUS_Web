import React, { useState } from 'react';

// A simple test component that displays the current website's header.
// This is a placeholder for better formatting to be done later
export const HelloKalmus = () => {
  const [todo, setTodo] = useState([]);

  return <h1>Hello Kalmus</h1>;
};
